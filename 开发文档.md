# 匿名评价应用 - 完整开发记录

## 项目概述

一个匿名互动主页应用，让用户生成自己的匿名主页和二维码，访客可以匿名留言、匿名聊天。

## 技术栈

- Next.js 14 (App Router)
- TypeScript
- TailwindCSS
- Supabase (数据库 + API)
- Vercel / Cloudflare Pages 部署

---

## 开发过程

### 第一阶段：项目初始化

#### 1. 创建项目结构

```bash
mkdir -p app/u/[slug] app/sent app/dashboard app/pay/[messageId] app/chat/[slug] components/ui lib public sql
```

#### 2. 安装依赖

```json
{
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@radix-ui/react-avatar": "^1.0.4",
    "@radix-ui/react-dialog": "^1.0.5",
    "@radix-ui/react-label": "^2.0.2",
    "@radix-ui/react-radio-group": "^1.1.3",
    "@radix-ui/react-slot": "^1.0.2",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "lucide-react": "^0.294.0",
    "nanoid": "^5.0.4",
    "next": "14.0.4",
    "qrcode.react": "^3.1.0",
    "react": "^18",
    "react-dom": "^18",
    "tailwind-merge": "^2.2.0",
    "tailwindcss-animate": "^1.0.7"
  }
}
```

### 第二阶段：Supabase 配置

#### 1. 创建 Supabase 项目

1. 访问 https://supabase.com 注册/登录
2. 创建新项目（选择美国区域）
3. 获取 API 密钥

#### 2. 数据库初始化 SQL

```sql
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 用户表
CREATE TABLE profiles (
  id UUID DEFAULT uuid_generate_v4() PRIMARY KEY,
  nickname TEXT NOT NULL,
  bio TEXT,
  slug TEXT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 留言表
CREATE TABLE messages (
  id UUID DEFAULT uuid_generate_v4() PRIMARY KEY,
  profile_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  mode TEXT NOT NULL CHECK (mode IN ('anonymous', 'revealable')),
  revealed BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 身份卡表
CREATE TABLE reveal_profiles (
  id UUID DEFAULT uuid_generate_v4() PRIMARY KEY,
  message_id UUID REFERENCES messages(id) ON DELETE CASCADE,
  nickname TEXT NOT NULL,
  contact_hint TEXT,
  intro TEXT,
  consent_reveal BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 访客表
CREATE TABLE visitors (
  id UUID DEFAULT uuid_generate_v4() PRIMARY KEY,
  profile_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
  visitor_token TEXT NOT NULL,
  nickname TEXT,
  avatar_url TEXT,
  contact_info TEXT,
  revealed BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 聊天表
CREATE TABLE chats (
  id UUID DEFAULT uuid_generate_v4() PRIMARY KEY,
  profile_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
  visitor_token TEXT NOT NULL,
  sender TEXT NOT NULL CHECK (sender IN ('owner', 'visitor')),
  content TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_profiles_slug ON profiles(slug);
CREATE INDEX idx_messages_profile_id ON messages(profile_id);
CREATE INDEX idx_messages_created_at ON messages(created_at DESC);
CREATE INDEX idx_reveal_profiles_message_id ON reveal_profiles(message_id);
CREATE INDEX idx_visitors_profile_id ON visitors(profile_id);
CREATE INDEX idx_visitors_visitor_token ON visitors(visitor_token);
CREATE INDEX idx_visitors_created_at ON visitors(created_at DESC);
CREATE INDEX idx_chats_profile_id ON chats(profile_id);
CREATE INDEX idx_chats_visitor_token ON chats(visitor_token);
CREATE INDEX idx_chats_created_at ON chats(created_at);

-- RLS 策略
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE messages ENABLE ROW LEVEL SECURITY;
ALTER TABLE reveal_profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE visitors ENABLE ROW LEVEL SECURITY;
ALTER TABLE chats ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Profiles are viewable by everyone" ON profiles FOR SELECT USING (true);
CREATE POLICY "Anyone can create profiles" ON profiles FOR INSERT WITH CHECK (true);
CREATE POLICY "Messages are viewable by everyone" ON messages FOR SELECT USING (true);
CREATE POLICY "Anyone can create messages" ON messages FOR INSERT WITH CHECK (true);
CREATE POLICY "Profile owners can update messages" ON messages FOR UPDATE USING (true);
CREATE POLICY "Reveal profiles viewable when message is revealed" ON reveal_profiles FOR SELECT USING (EXISTS (SELECT 1 FROM messages WHERE messages.id = reveal_profiles.message_id AND messages.revealed = true));
CREATE POLICY "Anyone can create reveal profiles" ON reveal_profiles FOR INSERT WITH CHECK (true);
CREATE POLICY "Visitors viewable by everyone" ON visitors FOR SELECT USING (true);
CREATE POLICY "Anyone can insert visitors" ON visitors FOR INSERT WITH CHECK (true);
CREATE POLICY "Profile owner can update visitors" ON visitors FOR UPDATE USING (true);
CREATE POLICY "Chats viewable by participants" ON chats FOR SELECT USING (true);
CREATE POLICY "Anyone can insert chats" ON chats FOR INSERT WITH CHECK (true);
```

#### 3. 环境变量配置

```env
NEXT_PUBLIC_SUPABASE_URL=https://xzxlkcdadweouokusejo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inh6eGxrY2RhZHdlb3Vva3VzZWpvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzgyNDc2OTYsImV4cCI6MjA5MzgyMzY5Nn0.USfn8iwtcpudXTAdRn_PoHdCaWLOvOOjDRWOdv_xTrQ
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

### 第三阶段：核心功能开发

#### 1. 首页 - 创建匿名主页

功能：
- 输入昵称
- 输入一句话介绍
- 生成匿名主页 slug
- 跳转到 /dashboard

#### 2. 用户主页 /u/[slug]

功能：
- 展示主页主人的昵称和简介
- 匿名留言（完全匿名 / 可解锁身份）
- 匿名聊天入口
- 记录访客

#### 3. 后台 /dashboard

功能：
- 展示主页链接和二维码
- 留言列表（支持解锁身份）
- 访客列表（支持解锁身份）
- 聊天列表

#### 4. 匿名聊天 /chat/[slug]

功能：
- 类似微信的聊天界面
- 实时消息
- 身份默认匿名

#### 5. 支付页 /pay/[messageId]

功能：
- Mock 支付（第一版）
- 解锁身份卡

### 第四阶段：部署

#### 方案 1：Vercel（需要 VPN）

```bash
# 安装 GitHub CLI
winget install GitHub.cli

# 登录 GitHub
gh auth login

# 创建仓库并推送
gh repo create anonymous-feedback --public --source=. --push

# 在 Vercel 导入项目并配置环境变量
```

**问题：** `*.vercel.app` 域名在中国大陆被墙，需要 VPN 访问。

#### 方案 2：Cloudflare Pages（推荐，国内可访问）

1. 注册 Cloudflare
2. 创建 Pages 项目
3. 连接 GitHub 仓库
4. 配置环境变量
5. 部署

---

## 项目结构

```
├── app/
│   ├── layout.tsx          # 根布局
│   ├── page.tsx            # 首页
│   ├── globals.css         # 全局样式
│   ├── u/[slug]/page.tsx   # 用户主页
│   ├── chat/[slug]/page.tsx # 匿名聊天
│   ├── sent/page.tsx       # 留言成功页
│   ├── dashboard/page.tsx  # 后台管理
│   └── pay/[messageId]/page.tsx # 支付页
├── lib/
│   ├── supabase.ts         # Supabase 客户端
│   ├── utils.ts            # 工具函数
│   ├── visitor.ts          # 访客 token 管理
│   └── actions.ts          # Server Actions
├── sql/
│   ├── init.sql            # 初始化 SQL
│   └── update.sql          # 更新 SQL
├── .env.example            # 环境变量示例
├── .env.local              # 本地环境变量
├── .gitignore              # Git 忽略文件
├── package.json            # 项目配置
├── tsconfig.json           # TypeScript 配置
├── tailwind.config.ts      # TailwindCSS 配置
└── README.md               # 项目文档
```

---

## 遇到的问题及解决方案

### 1. Supabase 表已存在

**问题：** 执行 SQL 时报错 `relation "profiles" already exists`

**解决：** 先执行 DROP 语句删除旧表，再执行创建语句。

### 2. 创建主页失败

**问题：** 点击"生成我的匿名主页"后没有反应

**解决：** 检查 Supabase 表是否创建成功，环境变量是否配置正确。

### 3. 二维码不显示

**问题：** 二维码只显示占位符

**解决：** 使用 `qrcode.react` 库生成真正的二维码。

### 4. 二维码扫码跳转失败

**问题：** 扫码后只显示一串数字字母

**解决：** 修复 `getBaseUrl()` 函数，使用 `window.location.origin` 返回完整 URL。

### 5. 微信扫码不能直接跳转

**问题：** 微信扫码后显示链接预览，需要点击才跳转

**解决：** 这是微信的正常行为，需要部署到公网后才能正常跳转。

### 6. Vercel 部署后无法访问

**问题：** `*.vercel.app` 域名在中国大陆被墙

**解决：**
- 方案 1：使用 VPN 访问
- 方案 2：绑定自定义域名
- 方案 3：部署到 Cloudflare Pages

### 7. GitHub CLI 安装问题

**问题：** `winget install GitHub.cli` 安装缓慢

**解决：** 等待安装完成，或手动下载安装。

### 8. Git 推送失败

**问题：** `Failed to connect to github.com port 443`

**解决：** 配置 Git 代理：
```bash
git config http.proxy http://127.0.0.1:7897
git config https.proxy http://127.0.0.1:7897
```

---

## 隐私说明

- 完全匿名留言不会留下可查看身份
- 只有访客主动选择可解锁身份卡，主页主人才可查看
- 访客记录使用浏览器生成的唯一标识，不收集真实信息
- 聊天身份默认匿名，主人无法看到访客真实身份
- 第一版不收集 IP、不识别访客

---

## 后续优化

1. **真实支付接入** - 替换 Mock 支付为微信支付/支付宝
2. **实时聊天优化** - 使用 Supabase Realtime 实现真正的实时通信
3. **访客身份解锁** - 完善访客身份卡信息收集
4. **域名备案** - 如果用户量上来，需要备案 + 国内服务器
5. **微信风控** - 匿名网站可能触发微信风控，需要优化

---

## 本地启动命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问 http://localhost:3000
```

## 部署命令

```bash
# 推送到 GitHub
git add .
git commit -m "Update"
git push

# Vercel 会自动部署
# 或手动在 Cloudflare Pages 触发部署
```

---

## 项目地址

- GitHub: https://github.com/Mike-li427/anonymous-feedback
- Vercel: https://anonymous-feedback-f7m8nly96-nju2.vercel.app (需 VPN)
- Cloudflare Pages: 待配置

---

## 开发时间线

1. **项目初始化** - 创建目录结构、配置文件
2. **数据库设计** - Supabase 表结构、SQL 初始化
3. **核心功能开发** - 首页、用户主页、后台、聊天
4. **部署配置** - GitHub、Vercel、Cloudflare
5. **问题修复** - 二维码、网络代理、域名访问

---

## 总结

本项目完成了一个完整的匿名互动主页应用，支持：
- 匿名留言（完全匿名 / 可解锁身份）
- 匿名聊天
- 访客记录
- 二维码分享
- Mock 支付

部署方案推荐使用 Cloudflare Pages + 自定义域名，可以在中国大陆直接访问。
