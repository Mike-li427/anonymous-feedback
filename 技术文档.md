# anonymous-feedback 技术文档

## 1. 项目概述

`anonymous-feedback` 是一个匿名互动产品原型，目标是让用户快速创建个人匿名主页，并通过链接或二维码接收匿名留言、匿名访客和匿名聊天。

当前版本已经具备以下基础能力：

- 创建匿名主页
- 公开主页留言
- 留言身份卡模式：`anonymous` / `revealable`
- 访客记录
- 匿名聊天
- 后台查看留言、访客、聊天
- Cloudflare Workers 部署链路

当前版本仍然带有明显的原型特征，主要体现在：

- 后台入口仍依赖浏览器 `localStorage` 中的 `profileId`
- 支付页仍是模拟支付流程
- 数据权限策略偏宽松，尚未收敛到生产级
- UI 和产品流程仍待升级为“扫码即玩、扫码管理”的正式版本

---

## 2. 业务目标

这个项目的长期方向已经比较明确：

- 不做传统注册登录
- 用“公开链接 + 私有管理链接 / 管理二维码”替代账号体系
- 用户创建主页后即可分享
- 访客扫码进入公开页
- 站主扫码进入管理页
- 对“可解锁身份”的留言，后续接入真实支付能力

换句话说，这个项目更适合做成轻量化、低门槛、偏传播型的匿名互动工具，而不是重账户体系的社区产品。

---

## 3. 当前技术栈

### 3.1 前端

- Next.js `16.2.6`
- React `19.2.6`
- TypeScript
- Tailwind CSS
- App Router

### 3.2 后端 / 数据

- Supabase
  - PostgreSQL
  - REST API
  - RLS

### 3.3 部署

- Cloudflare Workers
- OpenNext Cloudflare Adapter
- Wrangler

### 3.4 核心依赖

- `@supabase/supabase-js`
- `qrcode.react`
- `@opennextjs/cloudflare`
- `clsx`
- `tailwind-merge`

---

## 4. 项目目录结构

当前主要目录如下：

```text
app/
  globals.css                全局样式
  layout.tsx                 根布局
  page.tsx                   首页，创建匿名主页
  sent/page.tsx              留言成功页
  dashboard/page.tsx         当前后台页（依赖 localStorage）
  u/[slug]/page.tsx          公开匿名主页
  chat/[slug]/page.tsx       匿名聊天页
  pay/[messageId]/page.tsx   模拟支付页

lib/
  actions.ts                 Server Actions，业务主逻辑
  supabase.ts                Supabase 客户端与类型定义
  utils.ts                   slug / token / 时间 / URL 工具函数
  visitor.ts                 访客 token 管理

sql/
  init.sql                   初始建表脚本
  update.sql                 访客与聊天表补充脚本
  migration-v2.sql           owner_token 与 payment_orders 迁移脚本

public/
  _headers                   Cloudflare/OpenNext 相关头部配置

配置文件：
  package.json
  next.config.js
  wrangler.jsonc
  open-next.config.ts
  tsconfig.json
```

---

## 5. 页面与功能说明

### 5.1 首页 `/`

作用：

- 输入昵称和简介
- 调用 `createProfile()`
- 创建个人匿名主页

当前行为：

- 创建成功后把 `profile.id` 写入 `localStorage`
- 再跳转到 `/dashboard`

问题：

- 换设备后无法继续管理
- 与未来“扫码管理”目标不一致

### 5.2 公开主页 `/u/[slug]`

作用：

- 展示站主昵称与简介
- 访客提交匿名留言
- 支持两种留言模式：
  - `anonymous`：完全匿名
  - `revealable`：匿名留言，但允许未来被付费解锁身份卡
- 记录访客
- 跳转匿名聊天入口

### 5.3 匿名聊天 `/chat/[slug]`

作用：

- 访客与站主之间进行匿名聊天
- 基于 `visitor_token` 区分不同访客会话

当前特点：

- 聊天记录写入 `chats`
- 没有真正实时订阅，当前更接近“轮询前提下可读取”的消息模型

### 5.4 留言成功页 `/sent`

作用：

- 访客提交留言后的确认页
- 可返回首页，或回到对应公开主页

### 5.5 后台 `/dashboard`

作用：

- 展示个人主页链接和二维码
- 查看留言、访客、聊天列表
- 对留言 / 访客做“解锁身份”的模拟操作

当前问题：

- 依赖 `localStorage.profileId`
- 不适合扫码管理
- 缺少真正的私有后台密钥入口

### 5.6 支付页 `/pay/[messageId]`

作用：

- 当前用于模拟“支付后解锁身份”

当前状态：

- 仍是原型态
- 通过前端按钮触发 `mockRevealMessage()`
- 尚未接入真实订单、支付状态、回调校验

---

## 6. 当前数据模型

### 6.1 `profiles`

主表，用于存储站主主页信息。

当前字段：

- `id`
- `nickname`
- `bio`
- `slug`
- `created_at`

迁移规划 / 半落地字段：

- `owner_token`

说明：

- `lib/supabase.ts` 已经把 `owner_token` 写进 `Profile` 类型
- `sql/migration-v2.sql` 已经包含 `owner_token` 迁移脚本
- 但当前 `createProfile()` 还没有真正写入 `owner_token`
- 页面路由也还没有切换到 token 管理模式

这意味着：

- 类型和迁移已经开始为新方案铺路
- 但主业务流还没有真正切换

### 6.2 `messages`

用于存储访客对某个主页提交的留言。

关键字段：

- `profile_id`
- `content`
- `mode`
- `revealed`
- `created_at`

### 6.3 `reveal_profiles`

用于存储“可解锁身份”的身份卡信息。

关键字段：

- `message_id`
- `nickname`
- `contact_hint`
- `intro`
- `consent_reveal`

### 6.4 `visitors`

用于存储访客访问记录。

关键字段：

- `profile_id`
- `visitor_token`
- `nickname`
- `avatar_url`
- `contact_info`
- `revealed`

### 6.5 `chats`

用于存储匿名聊天消息。

关键字段：

- `profile_id`
- `visitor_token`
- `sender`
- `content`
- `created_at`

### 6.6 `payment_orders`

这是为真实支付预留的订单表。

当前状态：

- `sql/migration-v2.sql` 已包含建表脚本
- `lib/supabase.ts` 已包含 `PaymentOrder` 类型
- 但业务逻辑尚未真正接入

关键字段设计：

- `message_id`
- `profile_id`
- `amount`
- `status`
- `provider`
- `provider_order_id`
- `provider_transaction_id`
- `paid_at`

---

## 7. 核心代码逻辑

### 7.1 `lib/actions.ts`

这是目前最重要的业务入口，负责：

- 创建主页 `createProfile`
- 按 slug 查主页 `getProfileBySlug`
- 创建留言 `createMessage`
- 获取后台数据 `getDashboardData`
- 记录访客 `recordVisitor`
- 获取访客 `getVisitors`
- 发送聊天消息 `sendChatMessage`
- 获取聊天消息 `getChatMessages`
- 获取聊天概览 `getAllChats`
- 模拟解锁留言 / 访客

当前问题：

- `createProfile()` 只生成 `slug`，没有生成 `owner_token`
- `getDashboardData()` 仍按 `profileId` 查询，不是按私有 token
- 仍保留 `mockRevealMessage()` / `mockRevealVisitor()` 这类原型逻辑

### 7.2 `lib/supabase.ts`

负责：

- 初始化 Supabase 客户端
- 定义前端使用的数据类型

当前值得注意的点：

- `Profile` 已经声明了 `owner_token`
- 说明代码已经开始向 token 化后台演进
- 但这和 `actions.ts` 的实际插入逻辑暂时不一致

### 7.3 `lib/utils.ts`

当前包含：

- `generateSlug()`
- `generateOwnerToken()`
- `formatRelativeTime()`
- `getBaseUrl()`

当前状态说明：

- `generateOwnerToken()` 已经存在
- 但尚未真正接入创建主页流程

这也是一个典型的“功能已部分铺垫，但未贯通”的信号。

---

## 8. 环境变量

当前项目实际依赖的公开环境变量为：

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `NEXT_PUBLIC_SITE_URL`（本地 / 某些服务端 URL 兜底时使用）

注意事项：

- 文档中不应长期保存真实密钥值
- 尽管当前用的是 Supabase anon key，但仍建议只在 `.env.local`、Cloudflare 变量中配置
- 支付相关商户密钥未来必须使用新的服务端环境变量，不可暴露到前端

---

## 9. 部署现状

### 9.1 当前部署方式

当前仓库已经切换到 Cloudflare Workers 路线，而不是继续依赖 Vercel。

原因：

- 原项目有动态路由和服务端逻辑
- 更适合 Workers + OpenNext，而不是纯静态 Pages
- `vercel.app` 在中国大陆网络环境下可访问性较差

### 9.2 当前关键配置

- `package.json`
  - `build`: `next build --webpack`
  - `deploy`: `opennextjs-cloudflare build && opennextjs-cloudflare deploy`
- `wrangler.jsonc`
  - Worker 名称：`anonymous-feedback`
  - 开启 `nodejs_compat`
  - 已配置 Workers 资源绑定

### 9.3 当前已发布地址

当前已部署到：

- `https://anonymous-feedback.2309171175.workers.dev`

说明：

- 命令行部署已成功
- 但当前网络环境下，默认 `workers.dev` 域名仍可能无法稳定访问
- 要实现“国内更顺滑访问”，仍建议后续绑定自定义域名

### 9.4 部署中踩过的关键问题

1. Next.js 版本过低，Wrangler 无法自动配置
2. Google Fonts 拉取失败，影响构建
3. 新版 Next.js 对 `useSearchParams()` 的 `Suspense` 约束导致构建报错
4. Next 16 默认 Turbopack 在 Windows 下对动态路由构建不稳定
5. Wrangler OAuth 在 Windows 下使用默认 `localhost` 回调不稳定，改为 `127.0.0.1` 后登录成功

当前已经处理的结果：

- 升级到 Next 16 / React 19
- 移除对 Google Fonts 的依赖
- 构建切换到 `--webpack`
- Cloudflare 登录与部署链路可用

---

## 10. 当前架构状态判断

从代码真实状态来看，项目现在处于“原型功能已通，下一轮重构前夜”的阶段。

可以把当前状态拆成三层：

### 10.1 已经稳定落地的部分

- 创建主页
- 公开留言
- 匿名聊天
- 访客记录
- Cloudflare Workers 构建与部署

### 10.2 已经开始铺垫，但尚未打通的部分

- `owner_token` 管理后台方案
- `payment_orders` 真实支付方案
- 更正式的技术文档与交付资料

### 10.3 明确还没有完成的部分

- 扫码管理主流程
- 精美化 UI
- 真实支付接入
- 更严格的数据访问控制
- 正式自定义域名

---

## 11. 当前主要风险

### 11.1 后台入口风险

当前后台依赖 `localStorage.profileId`，意味着：

- 站主换设备后无法管理
- 链接分享闭环不完整
- 不符合“扫码管理”产品方向

### 11.2 数据权限风险

当前 RLS / policy 偏宽松，很多策略使用了 `USING (true)`。

这在原型阶段方便开发，但在生产环境存在明显风险：

- 数据可读范围过大
- “谁能更新什么”没有真正绑定身份
- 后续若上线真实支付，必须收紧权限

### 11.3 支付安全风险

当前支付是模拟实现，风险包括：

- 前端按钮即可改变解锁状态
- 没有订单状态机
- 没有服务端支付确认
- 没有回调校验

### 11.4 文案与字符编码风险

仓库中有部分中文文案出现编码异常，表现为页面和文档中的乱码字符串。

这会影响：

- 用户体验
- 文档可读性
- 后续交付给他人时的理解成本

---

## 12. 推荐的下一阶段改造顺序

### 第一优先级：切换到 token 管理后台

目标：

- 用 `owner_token` 替代 `localStorage.profileId`
- 创建主页后生成：
  - 公开链接
  - 私有管理链接
  - 两套二维码

这样能一举解决：

- 换设备管理问题
- 扫码即玩产品目标
- 后台分享与保存问题

### 第二优先级：支付架构落地

目标：

- 新增 `payment_orders` 真实业务流
- 去掉“模拟支付成功”
- 至少先把订单、状态、回调、解锁动作设计好

即使暂时没有真实商户参数，也应该先完成：

- 数据表
- 服务端接口
- 支付状态页
- 支付成功后的服务端解锁逻辑

### 第三优先级：UI 全面升级

重点页面：

- 首页
- 创建成功页
- 公开主页
- 后台
- 支付页

设计方向建议：

- 轻盈、现代、有产品感
- 更适合截图传播
- 明确区分“公开入口”和“管理入口”

### 第四优先级：RLS 与权限收口

当 token 后台和真实支付都接入后，应该尽快把 Supabase 权限策略从“开发方便”升级到“按角色 / 按 token / 按服务端操作”收口。

---

## 13. 本地开发与常用命令

### 安装依赖

```bash
npm install
```

### 启动开发环境

```bash
npm run dev
```

### 生产构建

```bash
npm run build
```

### Cloudflare 本地预览

```bash
npm run preview
```

### Cloudflare 部署

```bash
npm run deploy
```

---

## 14. 交接说明

如果后续由新的开发者继续接手，最需要先理解的不是 UI，而是这三件事：

1. 当前代码“表面上已经有 `owner_token` 和支付订单设计”，但主流程实际上还没切过去
2. 后台目前仍然是原型态，不是正式权限方案
3. Cloudflare 部署链路已经打通，因此后续改动可以直接沿着 Workers 路线继续，不必回退到 Vercel

一句话总结当前项目状态：

这是一个已经跑通匿名互动主流程、完成 Cloudflare 部署、并正在从 Demo 向正式产品过渡的 Next.js + Supabase 项目；下一步核心不是“再堆功能”，而是把后台入口、支付链路和产品体验真正做成闭环。
